{
  "name": "dsh-backup-plugin",
  "version": "0.2.1",
  "description": "Personal dsh backup plugin: one-click uncompressed tar snapshot of the DSH home, keeping the last N versions. v0.2 adds frontend settings (backup dir + keep) in the Settings menu.",
  "private": true,
  "type": "module",
  "main": "./lib/index.mjs",
  "exports": {
    ".": { "default": "./lib/index.mjs" },
    "./client": { "default": "./lib/client.cjs" },
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" },
    "client": { "platform": "web" }
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
